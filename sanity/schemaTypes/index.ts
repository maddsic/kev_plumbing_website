import about from '../schemas/about'
import banner from '../schemas/banner'
import logo from '../schemas/logo'
import services from '../schemas/services'

export const schemaTypes = [about, banner, logo, services]
